version: '3.7'

services:
  db:
    image: postgres:13.2
    container_name: postgres_db
    volumes:
      - ./docker/db:/var/lib/postgresql/data
      - ./postgresql.conf:/etc/postgresql/postgresql.conf
    expose:
      - "5432"
    ports:
      - "5454:5432"
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      retries: 5
      timeout: 5s
    restart: unless-stopped

  alembic:
    image: fastapi-base:latest
    user: root  # Chạy container với user root
    environment:
      SQL_DATABASE_URL: "postgresql+psycopg2://postgres:postgres@db:5432/postgres"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        pip install --no-cache-dir -r requirements.txt &&
        alembic upgrade head
    depends_on:
      db:
        condition: service_healthy

  app:
    image: fastapi-base:latest
    container_name: fastapi_app
    user: root  # Chạy với quyền root
    environment:
      SQL_DATABASE_URL: "postgresql+psycopg2://postgres:postgres@db:5432/postgres"
    ports:
      - "8000:8000"
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      alembic:
        condition: service_started

  localstack:
    container_name: "${LOCALSTACK_DOCKER_NAME:-parser-localstack-main}"
    image: localstack/localstack
    ports:
      - "127.0.0.1:4566:4566"            # LocalStack Gateway
      - "127.0.0.1:4510-4559:4510-4559"  # external services port range
    environment:
      # LocalStack configuration: https://docs.localstack.cloud/references/configuration/
      - DEBUG=${DEBUG:-0}
      - SERVICES=s3
    volumes:
      - localstack_data:/var/lib/localstack"
      - "/var/run/docker.sock:/var/run/docker.sock"
    networks:
      - spartan_net